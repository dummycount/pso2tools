<?xml version="1.0" encoding="utf-8"?>
<Page
	x:Class="Pso2Tools.Defrost.Views.Preview.PreviewPageText"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Pso2Tools.Defrost.Views.Preview"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:CommunityToolkit.WinUI.Controls"
	xmlns:preview="using:Pso2Tools.Defrost.ViewModels.Preview"
	mc:Ignorable="d"
>
	<Grid SizeChanged="Grid_SizeChanged">
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup x:Name="SizeGroups">
				<VisualState x:Name="Collapsed">
					<VisualState.Setters>
						<Setter Target="CategoryTabs.Visibility" Value="Collapsed" />
					</VisualState.Setters>
				</VisualState>
				<VisualState x:Name="Expanded">
					<VisualState.Setters>
						<Setter Target="CategoryComboBox.Visibility" Value="Collapsed" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>

			<VisualStateGroup x:Name="PageStates">
				<VisualState x:Name="SinglePage">
					<VisualState.Setters>
						<Setter Target="PageTabRow.Visibility" Value="Collapsed" />
					</VisualState.Setters>
				</VisualState>
				<VisualState x:Name="MultiplePages">
					<VisualState.Setters>
						<Setter Target="PageTabRow.Visibility" Value="Visible" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>

		<Grid.Resources>
			<DataTemplate x:Key="TextItemTemplate" x:DataType="preview:Pso2TextItemModel">
				<StackPanel Spacing="8">
					<TextBlock
						Text="{x:Bind Name, TargetNullValue='(Empty)'}"
						Style="{StaticResource BodyStrongTextBlockStyle}"
						Foreground="{ThemeResource TextFillColorTertiaryBrush}"
						IsTextSelectionEnabled="True"
						TextWrapping="NoWrap"
					/>
					<TextBlock
						Text="{x:Bind Text, TargetNullValue='(Empty)'}"
						IsTextSelectionEnabled="True"
						TextWrapping="WrapWholeWords"
					/>
				</StackPanel>
			</DataTemplate>

			<DataTemplate x:Key="RowTemplate" x:DataType="preview:Pso2TextRowModel">
				<ItemContainer>
					<ItemsControl
						ItemsSource="{x:Bind PageItems, Mode=OneWay}"
						ItemTemplate="{StaticResource TextItemTemplate}"
					>
						<ItemsControl.ItemsPanel>
							<ItemsPanelTemplate>
								<controls:UniformGrid ColumnSpacing="36" Rows="1" />
							</ItemsPanelTemplate>
						</ItemsControl.ItemsPanel>
					</ItemsControl>
				</ItemContainer>
			</DataTemplate>

			<Style TargetType="ItemContainer">
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="ItemContainer">
							<Grid
								x:Name="PART_ContainerRoot"
								Background="{TemplateBinding Background}"
							/>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
		</Grid.Resources>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<StackPanel Spacing="4" Padding="0,0,0,8">
			<StackPanel Orientation="Horizontal" Padding="24,0" Spacing="16" MinHeight="32">
				<TextBlock
					Text="Category"
					VerticalAlignment="Center"
					Style="{StaticResource BodyStrongTextBlockStyle}"
				/>
				<controls:Segmented
					x:Name="CategoryTabs"
					Style="{StaticResource PivotSegmentedStyle}"
					ItemsSource="{x:Bind viewModel.Categories, Mode=OneWay}"
					SelectedItem="{x:Bind viewModel.SelectedCategory, Mode=OneWay}"
					SelectionChanged="Category_SelectionChanged"
					Margin="0,-2,0,-4"
				>
					<controls:Segmented.ItemTemplate>
						<DataTemplate x:DataType="preview:Pso2TextCategoryModel">
							<TextBlock Text="{x:Bind Category, TargetNullValue='(Empty)'}" />
						</DataTemplate>
					</controls:Segmented.ItemTemplate>
				</controls:Segmented>

				<ComboBox
					x:Name="CategoryComboBox"
					ItemsSource="{x:Bind viewModel.Categories, Mode=OneWay}"
					SelectedItem="{x:Bind viewModel.SelectedCategory, Mode=TwoWay}"
				>
					<ComboBox.ItemTemplate>
						<DataTemplate x:DataType="preview:Pso2TextCategoryModel">
							<TextBlock Text="{x:Bind Category, TargetNullValue='Empty'}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</StackPanel>
			<StackPanel
				x:Name="PageTabRow"
				Orientation="Horizontal"
				Padding="24,0,24,0"
				Spacing="16"
				MinHeight="32"
				Visibility="Collapsed"
			>
				<TextBlock
					Text="Page"
					VerticalAlignment="Center"
					Style="{StaticResource BodyStrongTextBlockStyle}"
				/>
				<controls:Segmented
					x:Name="PageTabs"
					Style="{StaticResource PivotSegmentedStyle}"
					SelectionChanged="PageTabs_SelectionChanged"
					Margin="0,-2,0,-4"
				>
					<controls:Segmented.ItemTemplate>
						<DataTemplate x:DataType="x:Int32">
							<TextBlock Text="{x:Bind Converter={StaticResource IndexToPageConverter}}" />
						</DataTemplate>
					</controls:Segmented.ItemTemplate>
				</controls:Segmented>
			</StackPanel>
		</StackPanel>

		<ItemsView
			Grid.Row="1"
			x:Name="TextItems"
			ItemsSource="{x:Bind viewModel.TextRows, Mode=OneWay}"
			ItemTemplate="{StaticResource RowTemplate}"
			SelectionMode="None"
			Padding="24,12,24,24"
			CornerRadius="0"
		>
			<ItemsView.Layout>
				<StackLayout Spacing="24" />
			</ItemsView.Layout>
		</ItemsView>

		<ProgressRing
			Grid.Row="1"
			IsActive="{x:Bind viewModel.IsLoading, Mode=OneWay}"
			Visibility="{x:Bind viewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
		/>
	</Grid>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Application
	x:Class="Pso2Tools.Defrost.App"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Pso2Tools.Defrost"
	xmlns:converters="using:CommunityToolkit.WinUI.Converters"
	xmlns:tv="using:WinUI.TableView"
>
	<Application.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
			</ResourceDictionary.MergedDictionaries>

			<converters:StringFormatConverter x:Key="StringFormatConverter" />

			<Style TargetType="tv:TableViewHeaderRow">
				<Setter
					Property="Background"
					Value="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
				/>
				<Setter Property="BorderThickness" Value="0,0,0,0" />
				<Setter
					Property="BorderBrush"
					Value="{ThemeResource ControlStrokeColorDefaultBrush}"
				/>
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="tv:TableViewHeaderRow">
							<Grid
								x:Name="root"
								Background="{TemplateBinding Background}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								Padding="11,0,0,0"
							>
								<Grid.ColumnDefinitions>
									<ColumnDefinition x:Name="cornerButtonColumn" Width="Auto" />
									<ColumnDefinition
										x:Name="VerticalGridLineColumn"
										Width="Auto"
									/>
									<ColumnDefinition
										x:Name="FrozenHeadersPanelColumn"
										Width="Auto"
									/>
									<ColumnDefinition
										x:Name="ScrollableHeadersPanelColumn"
										Width="*"
									/>
								</Grid.ColumnDefinitions>

								<VisualStateManager.VisualStateGroups>
									<VisualStateGroup x:Name="CommonStates">
										<VisualState x:Name="Normal" />
									</VisualStateGroup>
									<VisualStateGroup x:Name="CornerButtonStates">
										<VisualState x:Name="NoButton" />
										<VisualState x:Name="SelectAllCheckBox">
											<VisualState.Setters>
												<Setter
													Target="selectAllCheckBox.Visibility"
													Value="Visible"
												/>
											</VisualState.Setters>
										</VisualState>
										<VisualState x:Name="SelectAllCheckBoxDisabled">
											<VisualState.Setters>
												<Setter
													Target="selectAllCheckBox.IsEnabled"
													Value="False"
												/>
												<Setter
													Target="selectAllCheckBox.Visibility"
													Value="Visible"
												/>
											</VisualState.Setters>
										</VisualState>
									</VisualStateGroup>
								</VisualStateManager.VisualStateGroups>

								<Grid x:Name="CornerButtonPanel">
									<CheckBox
										x:Name="selectAllCheckBox"
										Padding="0"
										Margin="10,0,0,0"
										MinWidth="20"
										MinHeight="20"
										IsTabStop="False"
										Visibility="Collapsed"
										VerticalAlignment="Center"
										HorizontalAlignment="Left"
									/>
								</Grid>

								<Rectangle
									x:Name="VerticalGridLine"
									Grid.Column="1"
									Width="1"
									VerticalAlignment="Stretch"
								/>

								<StackPanel
									x:Name="FrozenHeadersPanel"
									Grid.Column="2"
									Grid.Row="1"
									Orientation="Horizontal"
								/>

								<StackPanel
									x:Name="ScrollableHeadersPanel"
									Grid.Column="3"
									Orientation="Horizontal"
								/>

								<Border
									x:Name="ColumnDropIndicator"
									Grid.ColumnSpan="4"
									Visibility="Collapsed"
									HorizontalAlignment="Left"
									Translation="0, 0, 32"
									BorderThickness="1"
									RenderTransformOrigin="0.5,0.5"
									DataContext="{x:Null}"
									BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
									Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
								>
									<Border.Shadow>
										<ThemeShadow />
									</Border.Shadow>
									<Border.RenderTransform>
										<TranslateTransform x:Name="ColumnDropIndicatorTransform" />
									</Border.RenderTransform>
									<Grid>
										<FontIcon
											Glyph="&#xEB0F;"
											FontSize="12"
											Margin="0,-2,0,0"
											VerticalAlignment="Top"
											Foreground="{ThemeResource TextFillColorTertiaryBrush}"
										/>
										<FontIcon
											Glyph="&#xEB11;"
											FontSize="12"
											Margin="0,0,0,-2"
											VerticalAlignment="Bottom"
											Foreground="{ThemeResource TextFillColorTertiaryBrush}"
										/>
										<Image
											x:Name="DragHeaderImage"
											Stretch="None"
											Source="{Binding Visuals}"
										/>
									</Grid>
								</Border>
							</Grid>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
		</ResourceDictionary>
	</Application.Resources>
</Application>
